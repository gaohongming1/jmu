<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jmu.dp.center.dao.mapper.UserMapper">

    <select id="getInfo" parameterType="String" resultType="com.jmu.dp.center.dao.entity.UserBean">
        SELECT * FROM jmu_dp WHERE name = #{name} AND passWord = #{passWord}
    </select>
</mapper>